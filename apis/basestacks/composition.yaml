apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: basestacks.aws.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: aws.hops.ops.com.ai/v1alpha1
    kind: BaseStack
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-aws-base-stackrender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
